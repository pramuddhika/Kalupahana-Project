import OwnerPagesHeader from "../components/OwnerPagesHeader";

const Feedbacks = () => {
    return (
        <div>

          {/**page head -start */}
          <OwnerPagesHeader pageName="Feedbacks" />
          {/**page head - end */}

            <p>this is owner feedback page</p>
        </div>
    );
};

export default Feedbacks;