import OwnerPagesHeader from "../components/OwnerPagesHeader";

const Security = () => {
    return (
        <div>

          {/**page head -start */}
          <OwnerPagesHeader pageName="Security" />
          {/**page head - end */}

            <p>this is owner security page</p>
        </div>
    );
};

export default Security;